namespace BigRetail.Map.Fixtures
{
    /// <summary>
    /// Identifies the exact state mutation represented by a FixtureEdit.
    /// </summary>
    public enum FixtureEditKind
    {
        AddFixture = 0,
        RemoveFixture = 1
    }
}
