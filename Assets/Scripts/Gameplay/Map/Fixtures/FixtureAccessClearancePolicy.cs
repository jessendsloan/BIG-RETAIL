namespace BigRetail.Map.Fixtures
{
    /// <summary>
    /// Defines how many authored fixture faces must remain usable when the
    /// fixture is placed.
    /// </summary>
    public enum FixtureAccessClearancePolicy
    {
        AllAuthoredAccessPoints = 0,
        AtLeastOneCompleteSide = 1
    }
}
